"use client";

import Image from "next/image";
import Link from "next/link";
import { useState, useEffect } from "react";
import { products } from "@/data/products";

export default function Home() {
  // Use only the black T-shirt image from your screenshot
  const heroImage = products
    .find(p => {
      const colour = (p as any).colour?.toLowerCase() || '';
      const category = (p as any).productCategory?.toLowerCase() || '';
      return p.image && colour.includes('black') && category.includes('shirt');
    })?.image || products[0]?.image || '';
  
  // Specific images for category sections
  const mensImage = products.find(p => p.category === 'male' && p.image)?.image || '';
  const womensImage = products.find(p => p.category === 'female' && p.image)?.image || '';
  
  return (
    <div className="min-h-screen bg-white">
      {/* Hero Section - Single Image */}
      <section className="relative h-[90vh] bg-gradient-to-r from-gray-900 to-gray-700 overflow-hidden">
        <div className="absolute inset-0 bg-black/40 z-10"></div>
        {heroImage && (
          <Image
            src={heroImage}
            alt="True Religion - Premium Apparel"
            fill
            className="object-cover"
            priority
          />
        )}
        <div className="relative z-20 flex items-center justify-center h-full text-center text-white">
          <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
            <h1 className="text-4xl sm:text-5xl md:text-7xl lg:text-8xl font-bold mb-4 md:mb-6 tracking-tight bg-gradient-to-r from-red-600 to-blue-600 bg-clip-text text-transparent">
              TRUE RELIGION
            </h1>
            <p className="text-base sm:text-lg md:text-xl lg:text-2xl mb-6 md:mb-8 font-light max-w-3xl mx-auto leading-relaxed">
              Premium denim and apparel for the modern lifestyle. Discover authentic style with every stitch.
            </p>
            <div className="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center">
              <Link
                href="/products/male"
                className="bg-red-600 text-white px-6 sm:px-8 py-3 sm:py-4 font-semibold hover:bg-red-700 transition-colors text-sm tracking-wide min-h-[44px] flex items-center justify-center"
              >
                SHOP MEN
              </Link>
              <Link
                href="/products/female"
                className="bg-blue-600 text-white px-6 sm:px-8 py-3 sm:py-4 font-semibold hover:bg-blue-700 transition-colors text-sm tracking-wide min-h-[44px] flex items-center justify-center"
              >
                SHOP WOMEN
              </Link>
            </div>
          </div>
        </div>
      </section>

      {/* Featured Categories */}
      <section className="py-12 md:py-20 px-4 sm:px-6 lg:px-8">
        <div className="max-w-7xl mx-auto">
          <h2 className="text-3xl md:text-4xl lg:text-5xl font-bold text-center mb-8 md:mb-16 tracking-wide text-black">SHOP BY CATEGORY</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-6 lg:gap-8 max-w-4xl mx-auto">
            {/* Men's Category */}
            <Link href="/products/male" className="group">
              <div className="relative h-80 md:h-96 bg-gray-100 overflow-hidden rounded-lg">
                <Image
                  src={mensImage}
                  alt={"Men's Collection"}
                  fill
                  className="object-cover group-hover:scale-105 transition-transform duration-500"
                />
                <div className="absolute inset-0 bg-black/10 group-hover:bg-black/5 transition-colors duration-300"></div>
                <div className="absolute bottom-4 md:bottom-6 left-4 md:left-6 text-white">
                  <h3 className="text-xl md:text-2xl font-bold mb-1 md:mb-2 tracking-wide">MEN</h3>
                  <p className="text-xs md:text-sm opacity-90">Denim • Tees • Outerwear</p>
                </div>
              </div>
            </Link>

            {/* Women's Category */}
            <Link href="/products/female" className="group">
              <div className="relative h-80 md:h-96 bg-gray-100 overflow-hidden rounded-lg">
                <Image
                  src={womensImage}
                  alt={"Women's Collection"}
                  fill
                  className="object-cover group-hover:scale-105 transition-transform duration-500"
                />
                <div className="absolute inset-0 bg-black/10 group-hover:bg-black/5 transition-colors duration-300"></div>
                <div className="absolute bottom-4 md:bottom-6 left-4 md:left-6 text-white">
                  <h3 className="text-xl md:text-2xl font-bold mb-1 md:mb-2 tracking-wide">WOMEN</h3>
                  <p className="text-xs md:text-sm opacity-90">Denim • Tees • Active</p>
                </div>
              </div>
            </Link>
          </div>
        </div>
      </section>
    </div>
  );
}
