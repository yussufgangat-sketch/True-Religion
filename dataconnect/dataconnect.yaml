specVersion: "v1"
serviceId: "true-religion"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "true-religion-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
