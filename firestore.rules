rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow ALL access to EVERYTHING - completely open rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}